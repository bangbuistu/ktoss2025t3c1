#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n;
    cout << "Nhap mot so nguyen: ";
    cin >> n;
    if (n % 2 == 0) cout << n << " la so chan\n";
    else cout << n << " la so le\n";
    return 0;
}